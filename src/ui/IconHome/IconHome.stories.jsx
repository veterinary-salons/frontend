import IconHome from './IconHome';

export default {
  title: 'icon 24px/IconHome',
  component: IconHome,
};

const Template = (arg) => <IconHome {...arg} />;

export const Default = Template.bind({});

Default.args = {};
