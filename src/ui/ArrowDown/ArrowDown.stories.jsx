import ArrowDown from './ArrowDown';

export default {
  title: 'icon 18px/ArrowDown',
  component: ArrowDown,
};

const Template = (arg) => <ArrowDown {...arg} />;

export const Default = Template.bind({});

Default.args = {};
