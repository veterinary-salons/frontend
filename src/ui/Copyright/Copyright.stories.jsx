import Copyright from './Copyright';

export default {
  title: 'icon 18px/Copyright',
  component: Copyright,
};

const Template = (arg) => <Copyright {...arg} />;

export const Default = Template.bind({});

Default.args = {};
