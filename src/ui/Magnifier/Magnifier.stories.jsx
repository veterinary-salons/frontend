import Magnifier from './Magnifier';

export default {
  title: 'icon 24px/Magnifier',
  component: Magnifier,
};

const Template = (arg) => <Magnifier {...arg} />;

export const Default = Template.bind({});

Default.args = {};
