import GreenMark from './GreenMark';

export default {
  title: 'Icons/GreenMark',
  component: GreenMark,
};

const Template = (arg) => <GreenMark {...arg} />;

export const Default = Template.bind({});

Default.args = {};
