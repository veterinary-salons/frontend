import GearWheel from './GearWheel';

export default {
  title: 'Icons/GearWheel',
  component: GearWheel,
};

const Template = (arg) => <GearWheel {...arg} />;

export const Default = Template.bind({});

Default.args = {};
