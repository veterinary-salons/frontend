import ArrowDown from './ArrowDown';

export default {
  title: 'Icons/ArrowDown',
  component: ArrowDown,
};

const Template = (arg) => <ArrowDown {...arg} />;

export const Default = Template.bind({});

Default.args = {};
