import ArrowUp from './ArrowUp';

export default {
  title: 'Icons/ArrowUp',
  component: ArrowUp,
};

const Template = (arg) => <ArrowUp {...arg} />;

export const Default = Template.bind({});

Default.args = {};
