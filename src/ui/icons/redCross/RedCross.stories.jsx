import RedCross from './RedCross';

export default {
  title: 'Icons/RedCross',
  component: RedCross,
};

const Template = (arg) => <RedCross {...arg} />;

export const Default = Template.bind({});

Default.args = {};
