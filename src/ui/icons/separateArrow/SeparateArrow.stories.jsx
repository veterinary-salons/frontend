import SeparateArrow from './SeparateArrow';

export default {
  title: 'Icons/SeparateArrow',
  component: SeparateArrow,
};

const Template = (arg) => <SeparateArrow {...arg} />;

export const Default = Template.bind({});

Default.args = {};
