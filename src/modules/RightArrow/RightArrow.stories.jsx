import RightArrow from './RightArrow';

export default {
  title: 'icon 24px/RightArrow',
  component: RightArrow,
};

const Template = (arg) => <RightArrow {...arg} />;

export const Default = Template.bind({});

Default.args = {};
