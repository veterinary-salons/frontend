import ArrowRight from './ArrowRight';

export default {
  title: 'icon 18px/ArrowRight',
  component: ArrowRight,
};

const Template = (arg) => <ArrowRight {...arg} />;

export const Default = Template.bind({});

Default.args = {};
