import IconCar from './IconCar';

export default {
  title: 'icon 24px/IconCar',
  component: IconCar,
};

const Template = (arg) => <IconCar {...arg} />;

export const Default = Template.bind({});

Default.args = {};
