import GreenMark from './GreenMark';

export default {
  title: 'icon 24px/GreenMark',
  component: GreenMark,
};

const Template = (arg) => <GreenMark {...arg} />;

export const Default = Template.bind({});

Default.args = {};
