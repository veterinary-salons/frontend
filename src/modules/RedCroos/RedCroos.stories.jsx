import RedCroos from './RedCroos';

export default {
  title: 'icon 24px/RedCroos',
  component: RedCroos,
};

const Template = (arg) => <RedCroos {...arg} />;

export const Default = Template.bind({});

Default.args = {};
