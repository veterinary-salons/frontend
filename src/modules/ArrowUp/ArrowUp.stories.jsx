import ArrowUp from './ArrowUp';

export default {
  title: 'icon 18px/ArrowUp',
  component: ArrowUp,
};

const Template = (arg) => <ArrowUp {...arg} />;

export const Default = Template.bind({});

Default.args = {};
